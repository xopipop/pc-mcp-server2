# MCP SDK and core dependencies
mcp>=1.0.0
pydantic>=2.0.0
asyncio-mqtt>=0.16.0
python-dotenv>=1.0.0
loguru>=0.7.0
aiofiles>=23.0.0
typing-extensions>=4.8.0

# PC Control MCP Server - System monitoring and control
psutil>=5.9.8  # Required for system monitoring, process management
pyautogui>=0.9.54  # Optional - for GUI automation features

# Platform-specific dependencies
pywin32>=306; sys_platform == 'win32'  # Windows-specific operations

# Additional optional dependencies for extended features
# Uncomment as needed:
# cryptography>=42.0.0  # For secure operations
# watchdog>=3.0.0  # For file system monitoring
# pillow>=10.0.0  # Already included with pyautogui, for screenshots
# keyboard>=0.13.5  # For advanced keyboard control
# mouse>=0.7.1  # For advanced mouse control

# Excel-related dependencies (if using with excel-mcp-server)
# openpyxl>=3.1.2
# xlwings>=0.30.10
# pandas>=2.0.3
# numpy>=1.24.3
# matplotlib>=3.7.2 